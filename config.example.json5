{
  // 监听地址和端口
  "listening": "0.0.0.0:10010",

  // Bearer 认证 Token（自动生成或手动设置）
  "token": "your-secret-token-here",

  // 上游 HTTP 代理（可选，留空表示不使用代理）
  "http_proxy": "",

  // 是否跳过上游服务器的 TLS 证书验证
  "skip_tls": true
}
